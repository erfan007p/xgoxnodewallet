extends ../layout

block content
  .container
    if user
    .page-header
      h3(align='center')  New XGOX Address
      .newaddy #{address}
      .qrcode(align='center')
        a(href='xgox:' + address)
          img(src='' + data_url)
      br
      p(align='center')
        a.btn.btn-default(href='/wallet', role='button') Go back to your wallet       
